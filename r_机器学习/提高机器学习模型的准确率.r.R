###R中文社区，R语言微信公众号R实例
###R中文社区

###提高机器学习模型的准确率，建立更加稳健的模型
#增加更多的数据
#处理缺失值和异常值
#特征工程学（特征转换，或者提取新的特征）
#特征选择（所在领域的知识；可视化；参数统计；PCA）
#使用多种算法
#算法的调整
#集成模型（多个弱模型的结果组合在一起）
#交叉验证（十折交叉验证）

###在R中使用XGBoost算法（仅适用于数值型向量）
#极端梯度上升算法
library(xgboost)
rain=read.csv("")